{
  "rules": {
    "lobby": {
      ".read": "auth != null",
      "$code": {
        ".write": "auth != null && ((data.exists() && data.child('hostId').val() === auth.uid) || (!data.exists() && newData.child('hostId').val() === auth.uid))"
      }
    },
    "tables": {
      "$code": {
        "public": {
          ".read": "auth != null",
          ".write": "auth != null && (newData.child('hostId').val() === auth.uid || (data.exists() && data.child('hostId').val() === auth.uid))"
        },
        "private": {
          "$uid": {
            ".read": "auth != null && auth.uid === $uid",
            ".write": "auth != null && (root.child('tables').child($code).child('public').child('hostId').val() === auth.uid || (newData.child('holeCards').exists() && !root.child('tables').child($code).child('public').exists()))"
          }
        },
        "intents": {
          "$uid": {
            ".read": "auth != null && root.child('tables').child($code).child('public').child('hostId').val() === auth.uid",
            ".write": "auth != null && auth.uid === $uid"
          }
        },
        "emotes": {
          "$uid": {
            ".read": "auth != null",
            ".write": "auth != null && auth.uid === $uid"
          }
        }
      }
    }
  }
}
