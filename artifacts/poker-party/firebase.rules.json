{
  "rules": {
    "lobby": {
      ".read": "auth != null",
      "$code": {
        ".write": "auth != null && ((data.exists() && data.child('hostId').val() === auth.uid) || (!data.exists() && newData.child('hostId').val() === auth.uid))"
      }
    },
    "tables": {
      "$code": {
        "public": {
          ".read": "auth != null",
          ".write": "auth != null && data.child('hostId').val() === auth.uid"
        },
        "private": {
          "$uid": {
            ".read": "auth != null && auth.uid === $uid",
            ".write": "auth != null && root.child('tables').child($code).child('public').child('hostId').val() === auth.uid"
          }
        },
        "intents": {
          "$uid": {
            ".read": "auth != null && root.child('tables').child($code).child('public').child('hostId').val() === auth.uid",
            ".write": "auth != null && auth.uid === $uid"
          }
        },
        "emotes": {
          "$uid": {
            ".read": "auth != null",
            ".write": "auth != null && auth.uid === $uid"
          }
        }
      }
    }
  }
}
